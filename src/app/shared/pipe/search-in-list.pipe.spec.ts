import { SearchInListPipe } from './search-in-list.pipe';

describe('SearchInListPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInListPipe();
    expect(pipe).toBeTruthy();
  });
});
